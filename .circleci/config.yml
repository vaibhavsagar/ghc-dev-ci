version: 2
jobs:
  build:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - run:
          name: Install Nix
          command: install-nix.sh
      - run:
          name: Set up GHC development environment
          command: steps.sh

